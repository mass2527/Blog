import { <FTName | capitalize | replacelast('', 'Props')> } from './[FTName].type';

function <FTName | capitalize> ({ ...props }: <FTName | capitalize | replacelast('', 'Props')>) {
  return <div>
    <FTName | capitalize>
  </div>;
};

export default <FTName | capitalize>;
